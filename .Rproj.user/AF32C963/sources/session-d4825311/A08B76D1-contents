p_poisson_yi <- function(theta_v, m, x, c,
                         sample_size, n_cat){
  
  m_01 = ifelse(m == 1, 1, 0)
  model_y = theta_v[1] + theta_v[2] * x + theta_v[3] * m_01 + theta_v[4] * c
  exp_model_y = exp(model_y)
  
  response <- rpois(sample_size, exp_model_y)

  return(response)
}
